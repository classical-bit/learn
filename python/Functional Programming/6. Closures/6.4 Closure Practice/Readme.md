# Closure Practice
Doc2Doc should be able to add CSS styling to an HTML file. CSS uses selectors to indentify the HTML element to add the style property. Styles are essentially a chain of keys and values.

```css
p {
    color: red
}
```

- Selector: `p` (targets all <p> elements)
- Property: `color`
- Value: `red`
