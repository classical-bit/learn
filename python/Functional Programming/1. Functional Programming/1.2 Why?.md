# Why Python?
Frankly, Python is not the most "functional" programming language. It doesn't enforce static typing, immutability, or pure functions, and it lacks tail call optimizations.

So why use it? Well, one reason trumps all others: you already know Python. You can focus on functional concepts without learning a new language at the same time.
