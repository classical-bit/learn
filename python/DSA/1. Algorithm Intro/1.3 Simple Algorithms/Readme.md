# Simple Algorithms
If you've heard the stories about LeetCode and whiteboarding interviews, you might thing the word "alogorithm" os sunonumous with "hard problems."

__It's not__.

Some algorithms are quite simple, and some are quite complex. But they're all recipes for solving problems.

## Asignment
Let's calculate the "reach" of a group of LockedIn influencers so we can estimate how many views a post will get if they all reshare it.

__Complete the `summed` function__. It's a slightly modified version of the `add` algorithm from before, but instead of just two numbers, `a` and `b`, it accepts a list of numbers and returns the sum of all of them.
